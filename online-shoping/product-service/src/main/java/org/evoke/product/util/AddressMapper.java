package org.evoke.product.util;

import java.util.List;

import org.evoke.product.model.Address;
import org.evoke.product.model.Category;
import org.evoke.product.model.Product;
import org.evoke.product.model.User;

public class AddressMapper {

	
	/*public void map(List<Address> addresses,List<AddressTo> addressTos) {
		AddressTo addressTo =null;
		
		for (Address address : addresses) {
			addressTo = new AddressTo();
			
			addressTo.setCity(address.getCity());
			addressTo.setCountry(address.getCountry());
			addressTo.setId(address.getId());
			addressTo.setPincode(address.getPincode());
			addressTo.setState(address.getState());
			//addressTo.setUser(address.getUser());
			
		//	productsTos.add(productTo);		}
		
	}*/
	}



